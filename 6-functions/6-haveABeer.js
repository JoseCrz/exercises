// * create a function that validates the age of a person and returns `true` or `false` depending
// * on the age the function receives.
// * use the returned value to send a message that says something among the lines of:
// * 'you can have a beer' or 'you cannot have a beer'


const age = 25

const isAdult = ageValidator(age)

if (...) {
  ...
} else {
  ...
}