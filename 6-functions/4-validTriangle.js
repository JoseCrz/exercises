// * Create a function that recieves the 3 inner angles of a triangle and returns
// * `true` or `false` (booleans) depending whether that triangle would be valid or not

// ? Context:
// ? For a triangle to be valid the sum of it's 3 inner angles should be equal to 180

// ? For example a triangle with the following values would be valid
// ? angle1 = 60 
// ? angle2 = 60 
// ? angle3 = 60
// ? because 60 + 60 + 60 = 180

// remember that we can have any kind of logic inside a function,
// thus, we can use `if`s inside a function 👀
// Also, we can not only return a variable, but also an specific value
// For example, we can do this:

// return 10
// return 'hello'
// return true

const angle1 = 60 
const angle2 = 60 
const angle3 = 60

// const angle1 = 70 
// const angle2 = 50 
// const angle3 = 60

// const angle1 = 90 
// const angle2 = 50 
// const angle3 = 90