// * Create a function that receives 2 numbers and returns a array:
// * the first numbers is the first value of the array
// * the second number is the last value of the array
// * the returned array should contain all the numbers in between the first & second parameter

// ? Example:
// ? arrayCreator(8, 15)
// ? should return an array like this: [8, 9, 10, 11, 12, 13, 14, 15]