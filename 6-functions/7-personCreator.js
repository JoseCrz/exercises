// * Create a function that recieves the name, age, height and pet name of someone
// * and returns an Object with those same properties

// ? For example:
// ? personCreator('Jose', 24, 1.63, 'Mimi Bebé)
// ? should return an object called `person`
// ? and when we console.log(persona.name) the console should print 'José'